#include <stdio.h>

typedef struct
{
    int year, month, day;
}date;


int main() {
    date firstDate, secondDate;

    scanf("%d %d %d", &firstDate.year, &firstDate.month, &firstDate.day);
    scanf("%d %d %d", &secondDate.year, &secondDate.month, &secondDate.day);

    if()
    return 0;
}
